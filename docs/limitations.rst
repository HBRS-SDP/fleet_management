Limitations
=============

* The current configuration only allows one callback per message type. This is bound to cause problems whenever we have more than one thing reporting back (e.g. elevators, robots)
